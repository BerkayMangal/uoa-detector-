"""Live Alpha v1 webapp side (registry 5.25): storage, data reads, the job, the page.

The engine is pure (``uoa_detector.live_alpha``); this package owns every I/O.
Contract: ``docs/phase-5.25-live-alpha-acceptance.md``.
"""
